// skquad web app — landing page.
//
// TODO(phase-3): implement the SPA flows (onboarding, board, chat, agent
// panel, squad settings, metering, registry admin, audit). See docs/web-app-ux.md.
export default function Home() {
  return <main>skquad</main>;
}
