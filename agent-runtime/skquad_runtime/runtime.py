"""The agent runtime core loop.

TODO(phase-3): implement the lifecycle (boot -> idle -> working), task-scoped
context reset, the core loop (plan -> act via plugins -> observe -> complete),
LiteLLM calls through the LLM gateway, the plugin loader, the message inbox,
and the pgvector memory store. See docs/agent-runtime.md.
"""
